<?xml version="1.0" encoding="UTF-8"?>

<DataSources>

  <DataSource>

    <DataSourceName>
      PostgresDW
    </DataSourceName>

    <DataSourceDescription>
      Data Warehouse PostgreSQL
    </DataSourceDescription>

    <URL>
      http://localhost:8081/mondrian/xmla
    </URL>

    <DataSourceInfo>
      Provider=mondrian;
      Jdbc=jdbc:postgresql://host.docker.internal:5432/project_bi;
      JdbcUser=postgres;
      JdbcPassword=admin;
      JdbcDrivers=org.postgresql.Driver;
    </DataSourceInfo>

    <ProviderName>Mondrian</ProviderName>

    <ProviderType>MDP</ProviderType>

    <AuthenticationMode>
      Unauthenticated
    </AuthenticationMode>

    <Catalogs>

      <!-- FOODMART DEFAULT -->
      <!--
      <Catalog name="FoodMart">
        <Definition>
          /usr/local/tomcat/schema/foodmart.xml
        </Definition>
      </Catalog>
      -->

      <!-- PROJECT BI -->
      <Catalog name="Penelitian">

        <Definition>
          /WEB-INF/queries/penelitian.xml
        </Definition>

      </Catalog>

    </Catalogs>

  </DataSource>

</DataSources>